# gdzig-examples

Examples for gdzig

## Build

1. git clone https://github.com/gdzig/gdzig
2. cd gdzig/example
3. zig build run

## Options

1. -Dgodot-path=<godot_cmd> #default: use `godot` from PATH (use `-Dgodot-path=/path/to/godot` to override) 
